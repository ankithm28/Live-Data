name: CI

# Controls when the action will run. Triggers the workflow on push or pull request
# events but only for the master branch
on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]
    
jobs:
  build:
    name: Test
    runs-on: macOS-latest
    steps:
      - uses: actions/checkout@v1
      - name: Set up Node
        run: |
          cd spaced-master
          npm install -g react-native-cli
          npm install -g detox-cli
          npm install -g yarn
      - name: Install Detox
        run: |
          cd spaced-master
          brew tap wix/brew
          brew install applesimutils
      - name: Install Yarn Dependencies
        run: |
          cd spaced-master
          yarn install
      - name: Install Pod Dependencies
        run: |
          cd spaced-mastercd ./ios && pod install && cd ..
      - name: Detox Build
        run: |
          cd spaced-masterdetox build --configuration ios.sim.release
      - name: Detox Test
        run: |
          cd spaced-masterdetox test --configuration ios.sim.release --cleanup
